version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
  exclusions:
    presets:
      - std-error-handling

formatters:
  enable:
    - gofmt
